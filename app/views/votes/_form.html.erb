<%= form_with(model: vote) do |form| %>
  <% if vote.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(vote.errors.count, "error") %> prohibited this vote from being saved:</h2>

      <ul>
        <% vote.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :voter_id, style: "display: block" %>
    <%= form.text_field :voter_id %>
  </div>

  <div>
    <%= form.label :elections_id, style: "display: block" %>
    <%= form.text_field :elections_id %>
  </div>

  <div>
    <%= form.label :vote, style: "display: block" %>
    <%= form.number_field :vote %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
