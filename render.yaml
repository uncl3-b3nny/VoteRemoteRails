# databases:
#   - name: postgres
#   ipAllowList: []

# services: 
#  - type: web
#   name: rails
#   env: ruby
#   buildCommand: "./bin/render-build.sh"
#   startCommand: "bundle exec rails s"
#   envVars:
#   - key: RAILS_MASTER_KEY
#     sync: false
#   - key: DATABASE_URL
#   fromDatabase: 
#     name: postgres
#     property: connectionString
#   - type: redis
#     name: redis
#     ipAllowList: []
#     plan: free
#     maxmemoryPolicy: noeviction

databases:
  - name: postgresql
    databaseName: voteRemoteRails
    ipAllowList: []

services:
  - type: web
    name: voteRemoteRails
    runtime: ruby
    buildCommand: "./bin/render-build.sh"
    startCommand: "bundle exec puma -C config/puma.rb"
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: mysite
          property: connectionString
      - key: RAILS_MASTER_KEY
        sync: false